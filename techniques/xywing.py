from grid import Grid
from colours import tCol

def xywing(g):
    pass

### Notes
# XY-Chain
# Bi-value links
# Eliminate candidates that intersect both endpoints of the chain.


# 1. Find all bi-value cells:  len(g.getValid(x,y)) == 2
# 2. Find all bi-value pairs:  two bi-value cells that share one candidate.



# ERROR IN POINTING PAIRS.
